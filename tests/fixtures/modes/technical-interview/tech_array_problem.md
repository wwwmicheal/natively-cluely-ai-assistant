# Array problem

Find pair summing to target; sorted array required.

Input: a sorted integer array `nums` and an integer `target`.
Output: indices `[i, j]` (0-based) such that `nums[i] + nums[j] == target`, or `null` if no such pair exists.

Constraints: at most one valid pair. The array is sorted in non-decreasing order.
