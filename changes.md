- [src/components/AboutSection.tsx]: Updated "What's New" section to v2.5 — replaced v2.4 items with four new highlights: Modes Manager (7 specialized personas), Custom Context in Profile Intelligence, Real-Time Pro License Sync, STT Stability & Resilience; added LayoutGrid + User lucide imports
- [src/components/settings/HelpSettings.tsx]: Added "8. Modes Manager" accordion section covering all 6 non-General modes with descriptions, plus how-to guides for opening, activating, reference files, and custom modes; added Custom Context Notes subsection to "7. Pro Intelligence" explaining the textarea, usage, and examples; renumbered Miscellaneous to 9 and Stealth to 10; added LayoutGrid to lucide imports
- [electron/ipcHandlers.ts, electron/services/ModesManager.ts, electron/db/DatabaseManager.ts, electron/LLMHelper.ts]: Senior-level robustness pass on Modes Manager and Profile Intelligence — (1) added pro gate to modes:upload-reference-file, modes:delete-reference-file, and all 4 note-section write handlers (add/update/delete/remove-all) which were previously ungated, allowing free users to bypass the pro fence; (2) modes:update now blocks templateType changes to non-general templates and rejects all updates to existing non-general modes without pro — closing an indirect pro-bypass where a user could upgrade the auto-seeded general mode to sales/recruiting/etc.; (3) LLMHelper combined context cap: if KO profile block + mode reference block together exceed 60KB, mode context is trimmed with a warning log so the token budget for the actual user question is always protected; (4) DatabaseManager.setActiveMode now logs a warning when the target ID doesn't exist (0 rows changed) instead of silently leaving no mode active; (5) ModesManager file truncation off-by-one fixed — '\n[...truncated]' (14 chars) is now reserved before slicing so MAX_FILE_CHARS is never exceeded; (6) silent try/catch in both LLMHelper injection paths now logs the actual error message instead of swallowing it
- [src/App.tsx]: Trial → Standard conversion now clears the active mode via modesSetActive(null) so non-general mode prompts stop being injected after downgrade; Pro/Max/Ultra conversion keeps mode settings intact; BYOK already handled via clearActiveModeOnLicenseLoss
- [electron/ipcHandlers.ts, electron/preload.ts, premium/src/ModesSettings.tsx]: On license deactivation (manual deactivate, API key cleared, trial BYOK end), active mode is now reset to null in the DB via clearActiveModeOnLicenseLoss() so non-general mode prompts and reference files stop being injected into LLM calls; main process broadcasts modes-active-cleared event; preload exposes onModesActiveCleared listener; ModesSettings re-syncs its state on that event
- [electron/audio/NativelyProSTT.ts, electron/audio/DeepgramStreamingSTT.ts]: Fixed 1006 reconnect storm — (1) reconnectAttempts no longer resets on every connection open; backoff only clears after 5 s of stability so exponential delay actually increases (1.5s→3s→6s) instead of looping at minimum; (2) static per-key slot scheduler staggers concurrent connections (system vs mic) by 3 s to prevent simultaneous server hits from the same API key triggering concurrency-limit 1006s; (3) added unexpected-response handler and send error callbacks to DeepgramStreamingSTT for better diagnostics
- [electron/services/CredentialsManager.ts]: Fixed STT silently broken when sttProvider='none' but Natively key is present — getSttProvider() now self-heals to 'natively' and persists the fix; root cause: key cleared→re-entered via non-setNativelyApiKey path leaves provider stuck at 'none', causing createSTTProvider() to return null with zero logs
- [nativelyapi/server.js]: Fixed STT failover not triggering when all Deepgram keys are invalid — removed speculative markHealthy('deepgram') calls from key rotation paths (connect timeout handler and close handler); failure count now accumulates correctly across consecutive bad keys so providerHealth.deepgram.healthy becomes false after 3 strikes and switchToFallback fires to Google STT / ElevenLabs; markHealthy remains only in upstreamWs.on('open') (actual connection success) and shadow probe timeout (confirmed genuine silence)
- [electron/audio/DeepgramStreamingSTT.ts]: Fixed Deepgram BYOK 1006 disconnects — removed erroneous /3 decimation correction from setSampleRate() (audio is at native rate, not decimated); restored buffer flush on 'open' event; added keepalive=true URL param back (GitHub version had it; server-side idle timeout prevention); reduced KeepAlive interval to 1s (fires before Deepgram's ~1s idle window); log all non-Results/Metadata message types to diagnose any Deepgram error messages before close; close log now includes rate+lang for easier debugging
- [electron/ipcHandlers.ts, src/components/settings/ModesSettings.tsx, src/App.tsx]: Gated Modes behind Pro license — non-premium users see General mode only; ModesSettings renders a full-screen pro gate (ModesProGate) with locked mode card previews, upgrade CTA to checkout, Natively API link, and license activation hint; IPC handlers for modes:create, modes:delete, modes:set-active (non-general) now return error: 'pro_required' without a license; App.tsx passes isPremium + isTrialActive + onOpenNativelyAPI to ModesSettings
- [electron/db/DatabaseManager.ts, electron/ipcHandlers.ts, electron/preload.ts, src/types/electron.d.ts, electron/main.ts, premium/electron/knowledge/KnowledgeOrchestrator.ts, premium/electron/knowledge/ContextAssembler.ts, electron/LLMHelper.ts, src/components/SettingsOverlay.tsx]: Added Custom Context feature to Profile Intelligence — a free-form textarea where users can type any context for the AI (sales stats, leetcode solutions, preferences, etc.); auto-saved with 800ms debounce; injected as <user_context> XML block into all AI paths (premium KnowledgeOrchestrator + LLMHelper non-premium); persisted in SQLite via new profile_custom_notes table (migration v14); loaded on app startup and tab switch; 4000-char limit with live counter; works across all modes and providers
- [nativelyapi/server.js, electron/services/CredentialsManager.ts, electron/ipcHandlers.ts, electron/preload.ts, src/components/trial/FreeTrialBanner.tsx, src/components/trial/FreeTrialModal.tsx, src/components/settings/NativelyApiSettings.tsx, src/App.tsx]: Implemented 10-minute free trial system — HWID+IP-hash anti-abuse, server-issued signed tokens, per-trial quotas (10 AI / 10 min STT / 2 searches), guided trial start/status/convert endpoints, IPC layer, CredentialsManager trial token storage, FreeTrialBanner countdown UI, FreeTrialModal 3-path post-trial upgrade (Pro/Standard/BYOK with data wipe)
- [src/components/trial/FreeTrialModal.tsx]: Redesigned — featured Pro plan card with violet glow, 2-column secondary plan grid (Max/Standard), fixed DoneState dead-code bug (now shows with "Continue" button via onDone prop), cleaner BYOK row, wider modal (440px)
- [src/components/settings/NativelyApiSettings.tsx, src/App.tsx]: Fixed onByok/onDone split — wipe no longer closes modal prematurely; DoneState is now reachable
- [nativelyapi/server.js]: billAI/billSearch/billSTTSeconds now handle boolean return from atomic RPCs — cache-busts on DB-level quota guard trigger
- [src/components/onboarding/PermissionsToaster.tsx]: New premium glass card — first-launch TCC permissions request (screen recording + mic), macOS real status with Grant/Open Settings actions, Windows instruction variant, focus-listener re-checks status after System Preferences visit
- [src/components/trial/TrialPromoToaster.tsx]: New premium glass card — 5s delayed trial offer on non-first launches, violet aurora aesthetic, 10-min hero number, 3 feature chips, violet CTA + ghost dismiss, localStorage cooldown (7 days)
- [src/components/trial/FreeTrialModal.tsx]: Full redesign × 2 — Apple-grade card language: HeroCard (Pro) with full-width "Most popular" band, 30px price anchor, circular SF-green checkmarks, shimmer CTA; TierCard (Max/Ultra) with near-black cards, 4px dot features, hairline borders, single accent colour per plan; SlimCard (Standard) horizontal row; BYOK ghost row; inline ACC/C token system
- [electron/ipcHandlers.ts]: Added permissions:check (returns mic+screen TCC status, platform-aware) and permissions:request-mic (calls askForMediaAccess on macOS)
- [electron/preload.ts, src/types/electron.d.ts]: Added checkPermissions + requestMicPermission to ElectronAPI
- [src/App.tsx]: Wired PermissionsToaster (first launch gate) + TrialPromoToaster (restart, self-gating), TrialPromoToaster onStartTrial updates activeTrial state immediately
- [nativelyapi/server.js]: Added Deepgram key pool (DEEPGRAM_API_KEY + DEEPGRAM_API_KEY_1..5, up to 6 keys) with round-robin picker, per-key failure tracking, and session-scoped key rotation before Google STT failover
- [nativelyapi/server.js]: Added ElevenLabs key pool (ELEVENLABS_API_KEY + ELEVENLABS_API_KEY_1..5, up to 6 keys) with same round-robin/rotation pattern
- [nativelyapi/server.js]: Updated connectSTTProvider('deepgram') to pick from pool, store activeDeepgramKey in session scope, rotate to next key on non-1000 close before switching to Google STT
- [nativelyapi/server.js]: Updated connectSTTProvider('elevenlabs') same key pool pattern with rotation
- [nativelyapi/server.js]: Updated pickSTTProvider() to check key pool availability via side-effect-free hasAvailableDeepgramKey/hasAvailableElevenLabsKey helpers (prevents health checks from skewing round-robin counter)
- [nativelyapi/server.js]: Updated /health endpoint to show Deepgram/ElevenLabs pool status (N/M keys ready)
- [nativelyapi/server.js]: Fixed: markDeepgramKeyHealthy/markElevenLabsKeyHealthy now called on successful open to reset per-key strike counters
- [nativelyapi/server.js]: Fixed: Deepgram connect timeout now attempts key rotation before falling over to Google STT
- [nativelyapi/server.js]: Fixed: audio chunk size validation — oversized frames (>64KB) close session immediately
- [nativelyapi/server.js]: Fixed: language code not validated in WS auth frame — now uses same LANG_RE as /v1/chat, falls back to en-US
- [nativelyapi/server.js]: Fixed: Supabase error object not checked in webhook idempotency DB path — { error } now thrown to avoid silent dedup bypass
- [nativelyapi/server.js]: Fixed: activeSessions TOCTOU race — slot claimed synchronously immediately after check, sentinel overwritten with real startTime
- [nativelyapi/tests/key-pool.test.mjs]: Added 28-test stress suite — 13 unit (algorithm) + 15 integration (WebSocket) covering chunk limits, language validation, concurrency, key rotation, full Deepgram exhaustion → Google STT fallover, health endpoint pool status
- [src/components/trial/FreeTrialModal.tsx]: Card-level hover effects + full Apple/Tesla compact redesign — stripped feature lists to single Tesla-style spec line per card, inline "Popular" pill badge on Pro (no full-width band), price anchor inline with name, CTA heights reduced (36px Pro / 29px Tier / 28px Standard), BYOK row single horizontal line with inline arrow, all cards lift + border/glow brighten on hover with 220ms Apple cubic-bezier easing, "Best value" badge on Max, trust footer, "Start" CTAs
- [nativelyapi/server.js]: Fixed webhook billing bugs — payment.succeeded and subscription.renewed now guard against both DODO_NATIVELY_PRO_PRODUCT_ID and DODO_PRO_YEARLY_PRODUCT_ID so Natively Pro app buyers never accidentally receive API keys; intentional bundle (API Pro/Max/Ultra subscribers also get Natively Pro desktop access) is preserved
- [nativelyapi/server.js]: Fixed price-collision bug — Natively Pro desktop ($15) has same price as API Pro plan; detectPlan() now returns null for known Pro app product_ids (DODO_NATIVELY_PRO_PRODUCT_ID / DODO_PRO_YEARLY_PRODUCT_ID) so it can never accidentally return 'pro' for a desktop app purchase; all four detectPlan() call sites in handleApiWebhookEvent now null-check and abort with Telegram alert instead of issuing a wrong API key
- [nativelyapi/server.js]: Split single /webhooks/dodo into /webhooks/dodo/api (manages api_keys + bundle pro_licenses for Pro/Max/Ultra) and /webhooks/dodo/pro (manages pro_licenses only — one-time + Pro Yearly — can never create api_keys); shared sig-verify + idempotency extracted into registerWebhookRoute() helper; /webhooks/dodo now returns 410 + Telegram alert (misconfiguration detector); handleApiWebhookEvent has top-level Pro product guard so cross-configured events are rejected with alert instead of accidentally issuing API keys
- [nativelyapi/server.js]: Security hardening — (1) Gemini API key moved from URL query param to x-goog-api-key header (all 3 call sites) to prevent key leakage in proxy/CDN logs; (2) LANG_RE regex replaced \s with explicit space to block \n/\r/\t prompt injection via language parameter (3 locations: /v1/chat, /v1/chat/completions, WebSocket STT); (3) subscription.renewed now uses hasExplicitPlan guard before updating plan field — prevents silent plan downgrade to 'standard' when Dodo sends a renewal event without product_id or amount; (4) payment.failed handler now also checks DODO_PRO_YEARLY_PRODUCT_ID so Pro Yearly subscribers get the correct product name and checkout URL in failure emails; (5) admin /create-key endpoint no longer leaks Supabase error.message in the response body
- [nativelyapi/server.js]: Deep audit fixes — (1) billSTTSeconds cache eviction wrapped in try/finally so stale quota is never served even when RPC throws; (2) billAI in non-streaming /v1/chat separated from routeChat try/catch — billing failure no longer returns 503 after a successful AI response; (3) subscription.plan_changed now guards detectPlan with hasExplicitPlan check, alerts on missing data instead of silently downgrading, and correctly grants/revokes bundled Natively Pro license on upgrade/downgrade; (4) pro_licenses bundle insert now dedups by dodo_payment_id (subscription ID) instead of email — prevents duplicate license rows on concurrent webhooks; (5) payment.failed restricted to payload_type=Payment only — subscription renewal failures handled exclusively by subscription.on_hold to eliminate double recovery emails; (6) subscription.updated now alerts via Telegram on unrecognised Dodo status instead of silently ignoring it
- [nativelyapi/tests/test-payment-flows.mjs, nativelyapi/package.json]: Added 18-test payment integration suite covering every fixed scenario — signs real Dodo webhook payloads, fires at running server, verifies Supabase DB state; run with npm run test:payments (add --clean to delete test rows, --test N to run single test, --url to target production)
- [nativelyapi/server.js, nativelyapi/.env]: Added per-endpoint webhook secrets — DODO_WEBHOOK_SECRET_API (for /webhooks/dodo/api) and DODO_WEBHOOK_SECRET_PRO (for /webhooks/dodo/pro); both fall back to DODO_WEBHOOK_SECRET if not set so existing deployments keep working; verifyDodoSignature now accepts secret as parameter; registerWebhookRoute passes the correct secret per route
- [nativelyapi/server.js]: Split webhook into two dedicated endpoints (/webhooks/dodo/api and /webhooks/dodo/pro) via shared registerWebhookRoute infra — Pro app events (one-time + yearly) exclusively handled by handleProWebhookEvent which never touches api_keys; API plan events handled by handleApiWebhookEvent with isProAppProduct safety guard at the top; deprecated /webhooks/dodo returns 410
- [nativelyapi/server.js]: detectPlan() — added `return null` for unknown product_id (when pid is set but matches no configured product); amount heuristic now only runs when there is no product_id at all; all three handleApiWebhookEvent call sites (subscription.active, subscription.plan_changed, payment.succeeded) already guard on !plan and abort with Telegram alert
- [nativelyapi/server.js]: Fixed real-purchase bundling — Dodo omits product_id from payment.succeeded payload; added hard require: payment.succeeded at /api endpoint now rejects with Telegram alert if product_id is absent (amount fallback alone is ambiguous — $15 Pro app = $15 API Pro); also fixes DODO_WEBHOOK_SECRET_PRO mismatch causing /pro sig verification failures
- [nativelyapi/server.js]: handleProWebhookEvent subscription.active — "already active" branch now updates dodo_payment_id to the current subscriptionId; fixes edge case where a lifetime buyer later purchases a yearly sub causing renewal/expiry events to silently do nothing (they look up by subscriptionId but the row still had the old one-time paymentId)
- [nativelyapi/tests/emailTemplates.js]: Fixed crossSellProToApiEmailHtml plan tiers — updated from wrong (Standard $7.50, Pro $10, Developer $25) to correct (Standard $8, Pro $15, Max $25, Ultra $35) with accurate quota descriptions; added new crossSellStandardToUpgradeEmailHtml() function — violet-themed email for Standard subscribers showing API upgrade paths (Pro/Max/Ultra) + Natively Pro desktop (Lifetime + Yearly)
- [nativelyapi/server.js]: Updated cross-sell trigger logic — Standard subscribers now receive upgrade email (crossSellStandardToUpgradeEmailHtml) instead of Pro desktop email; Pro/Max/Ultra subscribers without Pro desktop still get crossSellApiToProEmail; added sendCrossSellStandardEmail() wrapper; added crossSellStandardToUpgradeEmailHtml to import
- [electron/ipcHandlers.ts]: Enhanced trial:end-byok step 5 — before SQL DELETE, now calls orchestrator.deleteDocumentsByType(DocType.RESUME/JD) to clear in-memory knowledge caches; prevents stale profile data from persisting in orchestrator after trial ends via BYOK
- [src/components/trial/FreeTrialModal.tsx]: Added onStandard prop — called fire-and-forget when user clicks Standard plan card, allowing caller to run wipe logic before checkout URL opens
- [src/App.tsx]: Wired onStandard on FreeTrialModal — wipes profile (resume + JD) when user picks Standard; also adds belt-and-suspenders profileDelete/profileDeleteJD calls after endTrialByok (orchestrator cache safety)
- [nativelyapi/server.js, nativelyapi/.env]: Added Tavily multi-key pool (TAVILY_API_KEY_1..11) with round-robin RR, per-key monthly credit tracking (1000 credits/key), automatic exhaustion sidelining until 1st of next month, Telegram alerts at ≤200 (⚠️ low) and ≤100 (🔴 critical) remaining plus 🚫 exhausted alert; fixed double-exhaustion-alert race via alertsSent guard; fixed operator-precedence bug in active-key count; /health endpoint now shows live Tavily pool status; fixed critical bug where 429 on one key returned 503 instead of retrying next pool key (bounded retry loop, bails only on network error or non-quota upstream error)
- [electron/audio/OpenAIStreamingSTT.ts]: Updated WebSocket session config to new OpenAI Realtime Transcription API spec — changed event type from `transcription_session.update` to `session.update` and restructured payload from flat fields to nested `audio.input.{format, transcription, turn_detection, noise_reduction}` (fixes issue #175)
- [electron/LLMHelper.ts]: Fixed custom/curl providers being ignored for note generation, meeting summarization, and suggestion generation — `generateMeetingSummary` now checks custom provider first (before Natively/Groq/Gemini) via direct `streamChat` with `ignoreKnowledgeMode=true` (prevents knowledge/profile intercept from corrupting summaries); `generateSuggestion` now routes custom/curl through `streamChat(lastQuestion, basePrompt)` with language injection applied exactly once and no conflicting HARD_SYSTEM_PROMPT
- [electron/ipcHandlers.ts]: Fixed profile intelligence (resume/JD upload, company research, negotiation, knowledge mode) being blocked for free trial users — added `isProOrTrialActive()` helper that checks both `LicenseManager.isPremium()` and unexpired trial token; replaced 5 hardcoded `LicenseManager.isPremium()` profile gates with this helper
- [src/components/settings/NativelyApiSettings.tsx]: Fixed two trial UI bugs — (1) trial activation card flashing on every settings open: added `isCheckingTrial` state that blocks the start card until `getLocalTrial` resolves + sets optimistic active state immediately from local token before server call; (2) usage counters stuck at 0: poll interval halved to 15s, `refreshTrial` now fires immediately without awaiting (no longer blocks the optimistic state render)
- [nativelyapi/server.js]: Fixed SyntaxError crash on startup — duplicate `const now` declaration in /v1/trial/start handler (line 2419 `Date.now()` + line 2482 `new Date()`); renamed line 2482 to `nowDate`
- [nativelyapi/server.js, electron/LLMHelper.ts, electron/audio/NativelyProSTT.ts, premium/electron/knowledge/NativelySearchProvider.ts, electron/ipcHandlers.ts]: Fixed trial usage counters never updating — root cause: all three Natively API clients sent `x-natively-key: __trial__` which the server rejected as `invalid_key_format` before checking the trial token, so requests failed silently and billing never ran. Fix: (1) server `authenticate()` and WS auth now skip key validation if key doesn't start with `natively_sk_` and fall through to trial token; (2) LLMHelper sends `x-trial-token` instead of `x-natively-key` when key is `__trial__`; (3) NativelyProSTT sends `trial_token` field in WS auth frame instead of `key` when `__trial__`; (4) NativelySearchProvider accepts optional `trialToken` param and sends `x-trial-token` header; (5) ipcHandlers passes trial token to NativelySearchProvider when key is `__trial__`
- [electron/ipcHandlers.ts]: Added `trial:wipe-profile-data` IPC — wipes orchestrator caches (resume+JD) + SQLite (company_dossiers, knowledge_documents, resume_nodes, user_profile) without clearing trial token or natively key; used for auto-wipe on expiry
- [electron/preload.ts, src/types/electron.d.ts]: Exposed `wipeTrialProfileData` in ElectronAPI
- [src/App.tsx]: Auto-wipe profile data when trial expiry is detected (first occurrence only, guarded by `profileWiped` flag); same wipe on already-expired token at launch; `onStandard` now calls `wipeTrialProfileData` (includes SQLite) instead of memory-only profileDelete calls; `onByok` simplified (endTrialByok already covers full cleanup); `PremiumUpgradeModal.onActivated` now also closes trial expired modal
- [src/App.tsx]: Pass `isTrialActive={!!activeTrial}` to SettingsOverlay
- [src/components/SettingsOverlay.tsx]: Added `isTrialActive` prop; derived `hasProfileAccess = isPremium || isTrialActive`; profile tab knowledge mode toggle and Persona Engine gated by `hasProfileAccess`; "FREE TRIAL" violet badge shown when trial active and not premium; "Upgrade" button variant for trial users (violet tint) instead of yellow "Unlock Pro"
- [premium/src/useAdCampaigns.ts]: Reworked ad engine — signature changed from `isPremium: boolean` to `planDetails: PlanDetails`; cooldown reduced from 4h to 1h; restart break (session-level flag means first ad of each session always allowed regardless of cooldown); circular queue now picks sequentially (sorted by priority then alpha) instead of randomly within same-priority group; plan-based targeting: max/ultra → no ads, pro (any provider) → only `max_ultra_upgrade` ad, standard/none → full ad set
- [premium/src/MaxUltraUpgradeToaster.tsx]: New gold/amber upgrade ad for Pro plan holders — Pro→Max/Ultra tier comparison, two-column plan cards (Max: Zap icon, gold; Ultra: Crown icon, purple), animated gold border, CTA "See Max & Ultra plans"
- [src/premium/index.tsx]: Added glob import + export for MaxUltraUpgradeToaster; updated nullAdCampaigns fallback signature to match new planDetails param
- [src/App.tsx]: Switched from isPremiumActive boolean to planDetails state (calls licenseGetDetails); passes planDetails to useAdCampaigns; renders MaxUltraUpgradeToaster for `max_ultra_upgrade` ad slot; refreshes planDetails on license activate/deactivate
- [premium/src/useAdCampaigns.ts, src/App.tsx]: Moved preview shortcuts to App.tsx with document.addEventListener (more reliable than window); added always-visible floating dev toolbar with one button per ad type (click to show card instantly, no keyboard needed); removed all DEV guards; exposed previewAd from hook; toasters render whenever activeAd is set
- [electron/main.ts]: Fixed mic activating in settings — reconfigureSttProvider() now only calls setupSystemAudioPipeline() when a meeting is active; outside a meeting the pipeline is deferred to startMeeting(), preventing MicrophoneCapture from eagerly creating a CPAL audio stream (which triggers the macOS orange mic indicator)
- [src/components/NativelyInterface.tsx]: Fixed screenshot not appearing in message bar during meetings — moved onScreenshotTaken/onScreenshotAttached listener registration from the [isExpanded] effect into a stable mount-only effect, preventing the race condition where ensure-expanded caused a panel-expand state flip that tore down the screenshot listeners just before the screenshot-taken event arrived
- [electron/main.ts]: Refactored global shortcut handlers for Cmd+H / Cmd+Shift+H — instead of taking screenshot in main process and firing fire-and-forget screenshot-taken/screenshot-attached IPC events, now forwards takeScreenshot/selectiveScreenshot actions to renderer via global-shortcut IPC so the renderer handles capture via the proven IPC invoke path
- [src/components/NativelyInterface.tsx]: Added takeScreenshot and selectiveScreenshot action handling in onGlobalShortcut effect — routes global shortcut actions to generalHandlers.takeScreenshot() / generalHandlers.selectiveScreenshot() which use IPC invoke (request/response) instead of fire-and-forget events
- [electron/ScreenshotHelper.ts]: Fixed assertScreenRecordingPermission throwing in dev mode — added !app.isPackaged bypass so Cmd+H/Shift+H work during development (same policy as the startup permission check in main.ts)
- [src/types/electron.d.ts]: Added missing `onSystemAudioPermissionDenied` to ElectronAPI interface to fix TS2339 build error
- [scripts/build-native.js]: Fixed getClangLibPath() to use `clang -print-resource-dir` first (works with both Xcode.app and Command Line Tools) before falling back to the Xcode.app hardcoded path — fixes `ld: library 'clang_rt.osx' not found` when building x86_64 cross-compile target on CLT-only systems
- [electron/ipcHandlers.ts, electron/main.ts, electron/preload.ts, src/components/SettingsOverlay.tsx]: Fixed verbose logging in packaged app — added `open-log-file`, `get-log-file-path` IPC handlers (shell.openPath to ~/Documents/natively_debug.log), `forward-log-to-file` fire-and-forget IPC so renderer console.log/warn/error are captured to the file when verbose is on; preload.ts patches renderer console and syncs verbose flag via `verbose-logging-changed` broadcast; setVerboseLogging now broadcasts to all windows; toggling verbose on shows a 5-second amber inline toast with file path and "Open" button + drain progress bar
- [package.json]: Bumped version from 2.3.2 to 2.4.0
- [src/components/AboutSection.tsx]: Updated "What's New" section to v2.4 — replaced 5 old items with 4 new user-facing highlights: 10-Minute Free Trial, Guided Permissions Setup, Reliable Screenshot Capture, Custom Provider Notes & Summaries; added Camera icon import
- [nativelyapi/server.js]: Fixed handleProWebhookEvent — replaced weak product_id guard (only rejected events when product_id was present-and-wrong) with two-tier guard: INSERT events (subscription.active, payment.succeeded) now REQUIRE product_id and validate it matches a known Pro product; UPDATE events (subscription.renewed/expired/failed) reject if product_id is present-and-wrong, allow-through if absent (safe: those events look up by subscriptionId which won't exist in pro_licenses for non-Pro subscriptions); prevents NativelyAPI Standard subscription.active events — with or without product_id — from incorrectly creating pro_licenses rows
- [electron/db/DatabaseManager.ts]: Added migration v10→v11 creating `modes` and `mode_reference_files` SQLite tables; added CRUD methods (getModes, getActiveMode, createMode, updateMode, deleteMode, setActiveMode, getReferenceFiles, addReferenceFile, deleteReferenceFile); default "General" mode seeded on first run
- [electron/services/ModesManager.ts]: New singleton service — wraps DB CRUD for Modes, defines 6 template system prompts (general/sales/recruiting/team-meet/looking-for-work/lecture), provides getActiveModeSystemPromptSuffix() and buildActiveModeContextBlock() for LLM injection
- [electron/ipcHandlers.ts]: Added 9 IPC handlers (modes:get-all, modes:get-active, modes:create, modes:update, modes:delete, modes:set-active, modes:get-reference-files, modes:upload-reference-file with PDF/DOCX/TXT extraction, modes:delete-reference-file)
- [electron/preload.ts]: Added ElectronAPI type definitions and ipcRenderer.invoke bindings for all 9 modes IPC channels
- [electron/LLMHelper.ts]: Injected active mode context into streamChat — active mode's system prompt suffix appended to base prompt, custom context and reference file contents prepended as context block before every LLM call
- [src/components/settings/ModesSettings.tsx]: New React component — Cluely-style split panel UI with mode sidebar (list, new button, templates shortcut), template gallery overlay (5 preset templates with icons/descriptions), mode editor (name, template type pills, personal context textarea with per-template placeholders, reference files list with upload/delete)
- [src/components/SettingsOverlay.tsx]: Added "Modes" tab to sidebar (SlidersHorizontal icon) between Profile Intelligence and AI Providers; renders ModesSettings in full-height layout without outer padding
- [electron/db/DatabaseManager.ts]: Added `mode_note_sections` table to v11 migration; added CRUD methods (getNoteSections, addNoteSection, updateNoteSection, deleteNoteSection, deleteAllNoteSections)
- [electron/services/ModesManager.ts]: Added ModeNoteSection type; TEMPLATE_NOTE_SECTIONS pre-defined per template (recruiting matches Cluely exactly); createMode now seeds default note sections; added getNoteSections, addNoteSection, updateNoteSection, deleteNoteSection, removeAllNoteSections
- [electron/ipcHandlers.ts]: Added 5 note section IPC handlers (modes:get-note-sections, modes:add-note-section, modes:update-note-section, modes:delete-note-section, modes:remove-all-note-sections)
- [electron/preload.ts]: Added type defs + bindings for 5 note section IPC channels
- [src/components/settings/ModesSettings.tsx]: Full redesign to match Cluely 1:1 — sidebar uses uniform FileText icons (not colored template icons), no template type pills in editor, template gallery shown at creation only, Notes Template section with editable sections (double-click to edit title/description, remove individual sections, "Remove template" to clear all, "+ Add section" inline), Reference files empty state matches Cluely (centered "Add files as real-time context." + paperclip Upload button), Save button floats top-right of content panel and only enables when context is dirty, InlineEdit component for double-click-to-edit mode names and section fields
- [electron/ipcHandlers.ts]: Added `settings:open-tab` IPC handler — finds launcher window and sends `settings:open-tab` event with tab name, then brings launcher to front
- [electron/preload.ts, src/types/electron.d.ts]: Added `openSettingsTab(tab)` invoke and `onOpenSettingsTab(cb)` listener to ElectronAPI
- [src/App.tsx]: Added `onOpenSettingsTab` listener that sets `settingsInitialTab` and opens SettingsOverlay; cleanup on unmount
- [src/components/NativelyInterface.tsx]: Added `LayoutGrid` import; added Modes button immediately after the SlidersHorizontal gear button — clicking it calls `openSettingsTab('modes')` to open the launcher's SettingsOverlay directly on the Modes tab
- [src/components/SettingsOverlay.tsx]: Removed Modes from the sidebar nav (no longer a manual tab); Modes panel still renders when activeTab === 'modes' (opened programmatically via the overlay button)
- [src/components/Launcher.tsx]: Added LayoutGrid import; added Modes button in the header right-side action bar, immediately before the Settings gear — calls onOpenSettings('modes') to open SettingsOverlay on the Modes tab directly
- [src/components/settings/ModesSettings.tsx]: Full pixel-perfect Cluely redesign — standalone two-column panel using inline styles; sidebar (240px, dark #0e0e0e bg) with X close, New Mode pill button, mode list with FileText icons + inline double-click rename + hover-reveal delete + blue circle-check for active mode, Cluely Templates at bottom; right panel with large 28px bold mode title, "Set active" pill button, "Real-time prompt" and "Notes template" boxes (show "Autofilled by Cluely" placeholder when empty, editable textarea when focused/filled), note sections as large rounded cards with hover-reveal delete, "+ Add section" row, reference files empty state with folder icon + description + "+ Add file" pill; accepts onClose prop for X button
- [src/App.tsx]: Added isModesOpen state; passes onOpenModes={() => setIsModesOpen(true)} to Launcher; renders ModesSettings in a standalone AnimatePresence modal (820×600, dark rounded panel with backdrop blur) separate from SettingsOverlay; click-outside dismisses
- [src/components/Launcher.tsx]: Added onOpenModes optional prop; LayoutGrid button now calls onOpenModes() instead of onOpenSettings('modes')
- [src/components/SettingsOverlay.tsx]: Removed ModesSettings import and modes tab render block; removed modes-conditional overflow class from content div
- [electron/services/ModesManager.ts, src/components/settings/ModesSettings.tsx]: Added 'Technical Interview' mode template — green Code2 icon, system prompt focused on DSA/system design/step-by-step thinking/progressive hints, note sections: Problem statement, My approach, Edge cases, Time & space complexity, Follow-up questions
- [electron/services/ModesManager.ts]: Updated 'sales' template note sections to match exactly: Action Items, Outcome, Prospect background, Discovery, Product, Objections
- [src/components/settings/ModesSettings.tsx]: Template modes (Sales, Recruiting, etc.) now show a collapsible "Natively's built-in prompt" disclosure below the real-time prompt box, displaying the full system prompt text; General mode shows nothing; textarea placeholder is now template-specific
- [src/components/settings/ModesSettings.tsx]: Add section UI redesigned — clicking "+ Add section" shows an inline card matching existing section card style, with "Section title" and "Instructions for Natively" placeholder fields; Enter confirms, Escape cancels; "+ Add section" button gains a pill border when active
- [src/components/settings/ModesSettings.tsx]: ... button now toggles a dropdown menu with "Delete Mode" (red trash icon) instead of deleting immediately; click-outside closes the menu to match exactly: Action Items, Outcome, Prospect background, Discovery, Product, Objections
- [electron/llm/prompts.ts]: Added 5 production-quality mode system prompts as named exports: MODE_LOOKING_FOR_WORK_PROMPT, MODE_SALES_PROMPT, MODE_RECRUITING_PROMPT, MODE_TEAM_MEET_PROMPT, MODE_LECTURE_PROMPT — each built on CORE_IDENTITY with numbered intent detection priority, per-question-type output format spec (including the [SAY THIS FIRST]/[THE CODE]/[AMMUNITION] structure for coding), explicit anti-pattern lists, and response format rules matching HARD_SYSTEM_PROMPT style
- [electron/services/ModesManager.ts]: Replaced inline stub prompts with imported named constants; removed CORE_IDENTITY import (now only used inside prompts.ts); Technical Interview and General remain empty (fall through to HARD_SYSTEM_PROMPT)
- [src/components/settings/ModesSettings.tsx]: Updated TEMPLATE_PROMPTS display strings to match new detailed prompts — structured summaries showing priority order, output formats, key rules, and forbidden patterns for each mode; added technical-interview entry
- [electron/services/ModesManager.ts]: Updated 'team-meet' template note sections to match exactly: Action Items, Announcements, Team updates, Challenges or blockers, Decisions made
- [electron/services/ModesManager.ts]: Updated 'looking-for-work' template note sections to match Cluely exactly: Follow-up actions, Overview, Questions and responses, Areas to improve, Role details
- [src/components/settings/ModesSettings.tsx]: Fixed "New Mode" button — now creates Untitled Mode directly (no template gallery); template gallery only via Natively Templates footer button. Real-time prompt is now always an open textarea (placeholder "Tell Natively how to respond during the conversation.") with inline Save button at bottom-right of the box. Added "..." dots button (circle, matches Cluely) next to Set active for mode deletion. Reference files empty state now matches screenshot: "Add files as real-time context." text + Upload file pill with Paperclip icon. Section order changed to: Real-time prompt → Reference files → Notes template. Replaced all "Cluely" strings with "Natively". Removed hover border brightening on note cards.
- [src/components/settings/ModesSettings.tsx]: Fixed "+ Add section" button always shows pill border (was incorrectly hiding border when card is active); updated general template real-time prompt placeholder to "Tell Natively how to respond during the conversation."
- [electron/LLMHelper.ts]: Fixed mode system prompt overriding HARD_SYSTEM_PROMPT entirely — mode prompt now appended as "## ACTIVE MODE" section so all core behavioral rules (anti-generic-response, output format, etc.) are preserved
- [electron/MeetingPersistence.ts, electron/db/DatabaseManager.ts, src/components/MeetingDetails.tsx]: Notes generation now mode-aware — when an active mode has note sections, generates a mode-specific JSON prompt asking the LLM to fill each template section; result stored as sections[] in detailedSummary; MeetingDetails renders sections dynamically when present instead of generic keyPoints/actionItems
- [premium/electron/knowledge/KnowledgeOrchestrator.ts]: Fixed ERR_MODULE_NOT_FOUND for NegotiationEngine by adding .js extension to dynamic import (ESM requires explicit extensions)
- [electron/MeetingPersistence.ts]: Fixed mode-specific notes: removed invalid JSON // comments from section prompt template (caused JSON.parse to fail silently); added explicit logging for active mode, section count, raw LLM response, and parsed sections; changed catch to warn instead of silent swallow
- [electron/db/DatabaseManager.ts]: Added v12 migration — seeds Summary/Action items/Key points note sections for the default General mode (was missing from v11, causing modeNoteSections to be empty for all users with existing installs)
- [electron/MeetingPersistence.ts]: Switched mode sections source from per-instance DB query to TEMPLATE_NOTE_SECTIONS[templateType] — more reliable (always uses canonical template, no DB seed dependency); General mode still uses generic summary; all other modes (sales, recruiting, team-meet, looking-for-work, lecture, technical-interview) generate notes following their template sections
- [electron/LLMHelper.ts]: generateSuggestion now mode-aware — loads active mode system prompt and uses HARD_SYSTEM_PROMPT + mode prompt when a mode is set; falls back to generic conversation coach prompt when no mode active; applies to all provider paths (Gemini, Groq, custom, Natively)
- [electron/MeetingPersistence.ts]: Removed `!== 'general'` guard — all modes (including General with Summary/Action items/Key points) now use mode-specific sections for notes generation
- [electron/MeetingPersistence.ts]: Notes generation now respects user-customized mode sections — queries DB sections first (user may have added/removed/renamed); only falls back to canonical TEMPLATE_NOTE_SECTIONS if the mode has no DB sections (e.g. newly created or default General)
- [electron/MeetingPersistence.ts]: Improved mode-specific notes prompt — removed ["bullet 1","bullet 2"] placeholders (LLM was copying them literally); injected mode's customContext when set; gracefully handles sections with no description; section JSON template uses [] instead of example values
- [electron/llm/prompts.ts]: Fixed CORE_IDENTITY — changed "as a candidate in interviews" to role-neutral "in interviews, sales calls, meetings, lectures, or any live conversation"; changed "INTERVIEW COPILOT" to "REAL-TIME COPILOT" so Sales/Recruiting/Team Meet modes don't get contradictory identity; added MODE_TECHNICAL_INTERVIEW_PROMPT (was empty string, now full 4-part coding format, system design, brainstorming, hints, and behavioral sections)
- [electron/services/ModesManager.ts]: Wired MODE_TECHNICAL_INTERVIEW_PROMPT into TEMPLATE_SYSTEM_PROMPTS (was '' causing it to fall through to the generic HARD_SYSTEM_PROMPT)
- [electron/db/DatabaseManager.ts]: Added v13 migration — backfills note sections for every existing mode instance that has 0 sections in mode_note_sections (covers Technical Interview and any other mode created before sections were seeded); scans all modes, checks for missing sections, inserts canonical template sections for that templateType
- [src/components/MeetingDetails.tsx]: fixed unclosed JSX conditional for overview block — added missing `)` after overview </div> to prevent TS error
- [electron/db/DatabaseManager.ts]: fixed migration v12 FOREIGN KEY crash — guard note-section insert with a mode existence check so it skips safely when mode_general_default was deleted
- [electron/llm/prompts.ts]: removed coaching-style 'SAY THIS' labels — renamed [SAY THIS FIRST]/[SAY THIS AFTER] to [Opener]/[Walk-through], removed [SAY THIS] from team meet prompt, added explicit CORE_IDENTITY rule banning coaching prefixes
- [electron/llm/prompts.ts]: removed all section label prefixes from coding format — spoken sentences now output as plain first-person text; only Follow-ups: bullets remain labeled; framing changed from 'scriptwriter' to 'you ARE the candidate'
- [electron/llm/prompts.ts]: Added <injected_context> block to all 7 mode prompts (General, Looking for Work, Sales, Recruiting, Team Meet, Lecture, Technical Interview) — each block teaches the LLM how to use <user_context>, <reference_file>, Profile Intelligence blocks (<candidate_experience>, <candidate_projects>, etc.), and <salary_intelligence> when present; blocks are mode-specific (e.g. Sales references product decks/pricing sheets, Recruiting cross-references JD against candidate answers, Technical Interview pulls from parsed resume for behavioral stories)
- [electron/LLMHelper.ts]: Fixed mode system prompt override clobbering KO profile intelligence — mode prompt now appends to the existing systemPromptOverride (from KnowledgeOrchestrator) instead of unconditionally replacing it with HARD_SYSTEM_PROMPT
- [src/components/SettingsOverlay.tsx]: Fix unterminated string constant in placeholder prop — changed double-quoted multiline string to template literal
- [electron/services/ModesManager.ts]: Added per-file cap (12k chars) and total cap (40k chars) to buildActiveModeContextBlock() — prevents context window overflow when large PDFs/docs are uploaded as reference files
- [electron/LLMHelper.ts]: generateSuggestion() now calls buildActiveModeContextBlock() and prepends mode context (reference files + custom context) to the transcript — reference files now actually work in the live suggestion path
- [electron/MeetingPersistence.ts]: Meeting notes prompt now uses buildActiveModeContextBlock() instead of just customContext — reference files (resume, product docs, etc.) are now used when generating notes
- [src/components/settings/ModesSettings.tsx]: Added Deactivate button to mode detail header — shows when mode is active, turns red on hover and calls modesSetActive(null); replaces static "Active" badge
- [src/components/settings/ModesSettings.tsx]: Added unsaved-changes guard to mode list click and Templates button — prompts to confirm before discarding unsaved context textarea edits
- [src/components/settings/ModesSettings.tsx]: Added maxLength=8000 and live character counter to custom context textarea; counter turns orange near the limit
- [src/components/NativelyInterface.tsx, electron/ipcHandlers.ts, electron/preload.ts, src/types/electron.d.ts]: Active mode name badge added to the Modes toolbar button — shows mode name inline; updates live via mode-changed IPC event broadcast on every set-active call
- [src/components/settings/ModesSettings.tsx]: Added template type selector pill row in mode detail panel — lets users switch template type after creation; reseeds note sections from the new template's defaults
- [electron/llm/prompts.ts]: Fixed Lecture prompt formula example — replaced unicode → with **Intuition:** label for consistent rendering
- [src/components/settings/ModesSettings.tsx]: Removed drifting TEMPLATE_PROMPTS constant and TemplatePromptDisplay component (~220 lines) — replaced with a single "Powered by Natively" line; added TEMPLATE_DEFAULTS for client-side note section reseeding
- [src/components/settings/ModesSettings.tsx]: Added reference file count badge (Paperclip icon + count) to sidebar mode items when referenceFileCount > 0
- [src/types/electron.d.ts]: Added full modes IPC type declarations to ElectronAPI interface
- [src/components/settings/ModesSettings.tsx]: Redesigned ModesProGate component with premium two-panel layout — left panel (42%) shows all 7 modes as a staggered list with per-mode color icons, taglines, lock indicators, and an "Active" badge on General; right panel (58%) features a glass upgrade card with ambient gold radial glow, inner border highlight, Natively Pro badge, "You're using 1 of 7 modes." headline, 4-item feature list with colored check icons, gold gradient "Get Pro" CTA and ghost "I already have a license" secondary; Emil-style cubic-bezier(0.23,1,0.32,1) easing, _mgFadeUp stagger animation (40ms per mode), translateY(-1px) hover on CTA; warm amber/gold (#d4a017) premium palette (no AI purple/neon); marketing psychology: loss aversion (visible locked modes), endowment effect (active badge), anchoring copy
- [src/components/settings/ModesSettings.tsx, src/components/NativelyInterface.tsx, src/components/Launcher.tsx]: Redesigned Modes icon and ModesProGate UI — replaced generic LayoutGrid lucide icon with custom SVG (2×2 grid of rounded squares, one filled solid = active mode, three outlined = available modes); replaced all lucide mode icons in gate with hand-crafted per-mode SVG paths (General=concentric circles, Interview=speech bubble, Sales=trend line+node, Recruiting=person silhouette, Team Meet=person cluster, Lecture=open book, Technical=code brackets); redesigned gate layout: General shown as full-width active row + 6 locked modes in 2-col grid (desaturated/dim at rest, hover reveals them at full opacity/color — purposeful animation that shows what's locked); right panel replaced with editorial headline ("6 / more modes.") replacing generic SaaS bullet list; CTA uses flat white-on-dark button (no gradient, no glow, scale(0.97) on press per Emil); all animations purposeful — hover on locked tiles (180ms ease), press feedback on CTA (120ms), close button color (150ms), nothing decorative
- [src/components/settings/ModesSettings.tsx]: Full redesign of ModesProGate to Apple-tier — removed two-panel split entirely; single centered column with 6 locked mode tiles in a flex row (each tile: 16px radius, hairline border, 22px custom SVG icon, name label, opacity 0.22 at rest, hover: opacity 0.7 + translateY(-2px) 200ms ease, gated behind media query hover:hover); headline "6 more modes." at 36px/700/-0.038em tracking; body copy at 14px/rgba(255,255,255,0.4)/1.65 line-height/max-width 440px; CTA is flat white-on-dark (rgba(255,255,255,0.92) bg, #0a0a0a text), scale(0.97) active only; header "Modes · Pro" uses dot separator + muted "Pro" label (no badge, no amber glow); font stack: -apple-system, BlinkMacSystemFont, SF Pro Display — renders as SF Pro on macOS
- [src/components/settings/ModesSettings.tsx]: Conversion-optimized ModesProGate redesign — two-panel layout: left 42% has loss-aversion headline ("The right mode for every conversation."), hook line, 3 specific benefit bullets (Interview/Sales/Recruiting with exact descriptions), +3 more, blue #3b82f6 CTA, ghost license link, trust footer; right 56% shows 6 fully-visible mode cards in 2×3 grid each with colored left-border accent, colored icon circle (per-mode hue), name, punchy tagline — Interview=indigo, Sales=emerald, Recruiting=amber, Team=pink, Lecture=sky, Technical=cyan; cards lift translateY(-2px) on hover; all 6 modes are VISIBLE not dimmed (users can't want what they can't see); BENEFITS array drives specificity-based desire; typography at SF Pro stack throughout
- [src/components/settings/ModesSettings.tsx]: Completely different ModesProGate approach — product preview design: left 36% has ultra-minimal copy ("This is Interview mode.") + "Natively structures your answers using STAR format automatically" + blue CTA + ghost license link + 5 other mode chips; right 64% shows a realistic faux-screenshot of Interview Mode in action (dark #1c1c1e card, Interview Mode header bar with indigo icon, interviewer question in italic, Natively's STAR-formatted response with Situation/Task/Action/Result labels, blinking cursor on last item, "+5 more" mode chips in footer); one-time mount animation (translateY 4px → 0, 320ms ease); blinking cursor on last STAR item adds life without decorative animation; the product preview replaces all icon grids and feature lists — users see exactly what they're missing
- [src/components/settings/ModesSettings.tsx, src/components/NativelyInterface.tsx, src/components/Launcher.tsx]: Complete premium redesign of Modes icon and ModesProGate — (1) New modes icon: hub-and-spoke mark (large center circle = active mode, 4 orbiting dots at N/E/S/W = other modes available), replaces generic LayoutGrid in toolbar and Launcher; (2) ModesProGate now uses single-column layout on #080808 deep background; (3) Mode icons redesigned as iOS squircles (96px, border-radius:22px) with per-mode linear gradients (violet→indigo, emerald, amber, pink, blue, cyan) and inner top highlight (inset 0 1px 0 rgba(255,255,255,0.22)) for 3D depth; (4) Hover: scale(1.06) translateY(-2px) + shadow intensification via CSS custom property per mode — purposeful (shows user what each mode IS, not just that it's locked); (5) Stagger entrance animation (mg-in-1 through mg-in-4, 40–260ms delays) appropriate since this is a first-time/rare screen; (6) Pill CTA button (height:48px, border-radius:24px) with blue gradient + inner glow, scale(0.98) on press; (7) System font (-apple-system) throughout; (8) All animations: transform+opacity only, cubic-bezier(0.23,1,0.32,1)
- [src/components/settings/ModesSettings.tsx, src/components/NativelyInterface.tsx, src/components/Launcher.tsx]: Replaced modes icon with proper "modes manager" mark — three filled rounded-rect tiles in a 2×2 grid with a "+" symbol where the fourth tile would be (user-specified design); clearly communicates "manage a collection of modes"; applied consistently across toolbar, Launcher header, and ModesProGate modal header
- [src/components/settings/ModesSettings.tsx, src/components/NativelyInterface.tsx, src/components/Launcher.tsx]: Refined modes manager icon to Apple-quality precision — upgraded to 16×16 viewBox for sub-pixel accuracy; tiles now 6×6px with rx=1.75 (29% corner radius, Apple squircle range); exact 2.5px gap between all elements; "+" uses separate <line> elements instead of path shorthand for cleaner stroke control; strokeWidth=1.75 on "+" matches visual weight of filled tiles; strokeLinecap="round" for Apple's signature soft tip termination
- [src/components/settings/ModesSettings.tsx]: Complete ModesProGate redesign — replaced gradient tile grid with double-bezel mode list (6 rows, accent bar + name + description + lock badge), switched to button-in-button white CTA with nested dark arrow circle, `#050505` OLED bg, editorial left-aligned copy, Vercel cubic-bezier entrance animation, no neon glows or gradient icons
- [src/components/settings/ModesSettings.tsx, src/components/NativelyInterface.tsx, src/components/Launcher.tsx]: Full redesign — new dial icon (circle + 3 mode-position dots at 12/4/8 o'clock) replaces tiles-plus mark in all 3 locations; ModesProGate rebuilt from scratch: #020202 pure-black bg, eyebrow "Natively Pro | 6 modes" with thin rule separator (no pill), headline "A different expert for every room." at 21px/-0.04em tracking, 2-column mode card grid (6 cards) each with bespoke stroke SVG icon (mic/trend/person/duo/slides/code brackets), card top-edge highlight pseudo-element for physical depth, staggered pg-c0–pg-c5 cascade animation (38ms apart, pg-card-reveal keyframe, cubic-bezier(0.32,0.72,0,1)), clean white CTA pill with translateY(-0.5px) hover + scale(0.97) active + arrow nudge right on hover via .pg-cta-arrow class; all old gate- classes removed; LayoutGrid import removed from ModesSettings.tsx; lock badges and decorative orbs removed; prefers-reduced-motion respected throughout
- [src/components/settings/ModesSettings.tsx, src/components/NativelyInterface.tsx, src/components/Launcher.tsx]: Reverted toolbar and Launcher modes icons back to three-tiles-plus (those were changed without authorization in previous step — only the ProGate was in scope)
- [src/components/settings/ModesSettings.tsx]: Complete ProGate redesign — new "Persona Stack" logo mark (44px SVG: three fanned rounded-square cards at 0°/6°/12° rotation with 2×2 dot grid in front card — top-left active, three dimmer = available); Apple-pattern mode chips (6 plain text pills, no icons, no descriptions — recognition creates desire, loss aversion); all per-mode stroke icons and card grid removed; animations: logo enters scale(0.88→1) with cubic-bezier(0.23,1,0.32,1) at 30ms, hero slides up at 80ms, chips cascade 25ms apart (200–325ms), CTA at 360ms; CTA hover: translateY(-0.5px) + deep shadow; CTA active: scale(0.97) at 80ms; arrow nudge: translateX(2px) on hover; Lock + ArrowRight lucide imports removed
- [src/components/NativelyInterface.tsx, src/components/Launcher.tsx]: Modes toolbar icon: replaced "+" cross (two line elements) with a hollow rounded-square outline (rx=1.75, strokeWidth=1.5) in the bottom-right position — icon now reads three filled tiles + one outline tile (■ ■ / ■ □) instead of three tiles + plus sign
- [src/components/settings/ModesSettings.tsx]: Complete ground-up ModesProGate redesign — cinematic Apple/Tesla aesthetic, AIDA psychology sequence: (1) Ambient glow fades in 600ms (spotlight-on-stage effect), (2) PersonaStackMark logo scales from 0.88 at 60ms cubic-bezier(0.23,1,0.32,1), (3) "Pro Modes" barely-visible eyebrow at 220ms, (4) Headline "Every conversation. A different expert." at 280ms, (5) Sub copy at 340ms, (6) Mode name rows (typographic, not chips) cascade at 400/450ms — loss aversion: visible but unreachable, (7) CTA at 490ms; design details: #010101 bg, radial-blur ambient glow (filter:blur 12px, not neon), 32px centered hairline breath mark, mode names as "Interview · Sales · Recruiting" typography (no borders/backgrounds), CTA magnetic hover (translateY -0.5px + deep shadow), arrow nudge translateX(2.5px), scale(0.97) press at 80ms, all centered layout
- [src/components/settings/ModesSettings.tsx]: Replaced plain typographic mode name rows with premium 2-column card grid (6 cards: Interview/Sales/Recruiting/TeamMeet/Lecture/Technical) — each card has: bespoke 13×13 stroke SVG icon, mode name (11px/600), short descriptor (9.5px/muted), per-mode top-edge color accent gradient (purple/green/amber/pink/sky/cyan), top-sheen physical depth layer, glass bg (rgba white 0.04/border 0.09); staggered entrance animations mpg-c0–c5 with scale(0.96)+translateY(5px) from at 380–530ms; card hover: bg brighten + border lighten + translateY(-1px) at 140ms; removed ROW_A/ROW_B constants, hairline separator, mpg-row keyframe, mpg-rowA/rowB classes
- [src/components/settings/ModesSettings.tsx]: ProGate redesign — bg #010101→#1a1a1a (warm dark grey); mode cards replaced with color-identity card system (UX principle: specificity>generality; Linear/Vercel label pattern): each card has unique hex color (violet/emerald/amber/rose/sky/cyan), 2px left-edge rail at 55% opacity, tinted bg (hex+'0d'), colored border (hex+'28'), small-caps category label (HIRING/REVENUE/TALENT/MEETINGS/LEARNING/TECHNICAL) in mode color, bold mode name (12.5px/700/-0.025em) as hero — no generic stroke icons; hover via inline onMouseEnter/Leave: bg→hex+'18', border→hex+'50', translateY(-1px), colored box-shadow; ambient glow softened for grey bg; card CSS transitions updated (160ms ease + box-shadow)
- [src/components/settings/ModesSettings.tsx]: removed Template type selector from mode edit panel (only relevant at creation time); removed dead changeTemplateType function
- [src/components/settings/ModesSettings.tsx]: redesigned Active/Set-active button and dots menu to match reference design — solid pill, no borders, cyan active state, coral delete color
- [src/App.tsx, electron/ipcHandlers.ts]: wire onLicenseStatusChanged in App.tsx to reactively update isPremiumActive; broadcast license-status-changed from license:activate and license:deactivate IPC handlers
- [src/components/settings/ModesSettings.tsx]: reduced Delete Mode button size (padding, font, icon, gap, minWidth)
- [electron/ipcHandlers.ts]: Fixed Soniox API key test — valid keys no longer time out; now resolves success after 2.5s of silence post-config (server only replies on error, not on valid key)
- [electron/LLMHelper.ts]: Fixed generateContentStructured to include custom/cURL providers (OpenRouter etc.) in the fallback chain — profile intelligence and structured gen features now work when only a custom provider is configured
